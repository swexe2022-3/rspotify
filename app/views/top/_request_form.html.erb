<%= form_with model: @request, url: "/music_requests", method: :post do |f| %>
    <% if @friends.size != 0 %>
            <%= f.select :user_spotify_id, @friends %>
            <%= f.hidden_field :friend_spotify_id, :value => current_user.spotify_uid %>
            <%= f.hidden_field :music_title, :value => track.name %>
            <%= f.submit "友達に曲をリクエスト" %>
    <% end %>
    <%= f.select :user_spotify_id, @friends %>
    <%= f.hidden_field :friend_spotify_id, :value => current_user.spotify_uid %>
    <%= f.hidden_field :music_title, :value => @track.name %>
    <%= f.submit "友達に曲をリクエスト" %>
<% end %>