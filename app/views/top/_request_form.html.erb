
<% if not @friends.nil? %>
    <%= form_with url: "/music_requests", method: :post do |f| %>
        <% #友達がいない時は表示されない %>
        <%= f.select :user_spotify_uid, @friends %>
        <%= f.hidden_field :music_title, :value => @track %>
        <%= f.submit "これ聞いて！" %>
    <% end %>
<% end %>